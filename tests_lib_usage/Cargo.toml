[package]
name = "logic-mesh-lib-test"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
logic-mesh = { path = "..", features = ["multi-threaded"] }
tokio = { version = "^1.50", features = ["rt", "rt-multi-thread", "macros"] }
